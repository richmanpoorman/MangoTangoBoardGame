public enum GamePhase {
    PLACE, MOVE 
}

public enum PlayerColor {
    PLAYER_1, 
    PLAYER_2,
    BLANK, 
    MISSING
};

public enum PieceType {
    TILE, 
    SCOUT,
    BLANK,
    MISSING
};

public enum PlayerType {
    LOCAL, ONLINE, AI
};